import { ComingSoon } from "@/components/coming-soon"

export default function EquipmentPage() {
  return (
    <ComingSoon
      title="Equipment"
      description="A searchable, filterable table of every asset — by type, status, and department — is next up."
    />
  )
}
